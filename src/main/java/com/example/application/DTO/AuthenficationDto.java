package com.example.application.DTO;

public record AuthenficationDto(String username, String password) {
    // This class is a DTO (Data Transfer Object) used to transfer data between the client and the server
}
